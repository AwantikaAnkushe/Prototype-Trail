we built a prototype of Source Code Secret Detection & Blocking using LLMs. 
We developed a Chrome Extension that detects and blocks sensitive data before it is shared with AI tools at the browser level. 
Our core logic was based on ML Inference Pipeline, Regex and LLMs/CodeBERT.
